import React from 'react';
import { useNavigate } from 'react-router-dom';

const Pets = () => {
    const navigate = useNavigate();

    return (
        <div className='w-screen h-screen bg-[#fbfcfc] '>
            {/*Aquí puedes añadir el contenido de la página*/}
        </div>
    );
};

export default Pets;