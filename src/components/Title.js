import React from 'react';

const Title = ({ text }) => {
    return (
        <h1 className="w-auto h-16 text-center text-black text-5xl font-normal font-['Kefa']">{text}</h1>
    );
};

export default Title;