import React from 'react';
const FormCommentLink = ({ text1, text2, href }) => {
    return (
        <p className="text-center">
            <span style="text-black text-base font-normal font-['IBM Plex Sans'] leading-normal tracking-tight">{text1}</span>
            <span href={href} style="text-sky-400 text-base font-normal font-['IBM Plex Sans'] leading-normal tracking-tight">{text2}</span>
        </p>
    );
};

export default Button;