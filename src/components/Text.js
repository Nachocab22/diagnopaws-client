import React from 'react';
const Text = ({ text }) => {
    return (
        <span className="text-center text-black text-base font-normal font-['IBM Plex Sans'] leading-normal tracking-tight">{text}</span>
    );
};

export default Text;